package TrabalhoPratico;
import java.util.*;
public class Main {

	public static void main(String[] args) {
	
	/*	
	    int palavras = 1;
	    String nome,arroz;
	    String escolhida;
	    while(!= EOF){
	        // Parte da Extração
	        nome = resultado_extração
	        if(nome.length() <=  16){
	            for(i=0;i<=nome.length();i++){
	                arroz[i] = nome[i];
	            }
	            (i=nome.length(); i<=16;i++){
	                arroz[i] = ' ';
	            }
	        }
	        insere(arroz);
	        Extrai palavra
	        palavra++;
	        else{
	           nome[17] = '\0'
	           inserir(nome);
	        }
	        System.out.println("------------------------------MENU---------------------------");
	        System.out.println("Digite 1 para escolher uma palavra para ser pesquisada");
	        System.out.println("Digite 0 para sair da aplicação");
	        switch(op){
	            case '1':
	            {
	                escolhida = in.nextString();
	                Pesquisa(escolhida);
	                System.out.println("A palavra escolhida foi:" + escolhida);
	                System.out.println("Palavra com " + ocorrencias +  " ocorrencias);
	                Imprimir vector ou array list 
	            }
	        }
	        
	    }
	*/
	    /* --------------- STEPS --------------------
	        Criar contador de palavras (Começa no 1)
	        Extrair a palavra do texto
	        Aumentar pra ela ter 16 letras /Caso for maior que 16 letras particionar em duas
	        Inserir ela na Árvore Patricia
	        Adicionar o contador de palavras (palavras ++)
	        Assim vai até o final do arquivo
	        Qual palavra deseja buscar no texto?
	        
	    */	
		
	}

}
